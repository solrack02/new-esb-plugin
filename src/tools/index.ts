

import React from 'react';
import { Text, View } from 'react-native';
 
const elements = {
    DynView
};
 export const tools = () => {
  console.log('Welcome to the tools!');

  const returnObj = {
    elements,
  };

  return returnObj;
};
